import { ItemTextTrimmerDirective } from './item-text-trimmer.directive';

describe('ItemTextTrimmerDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemTextTrimmerDirective();
    expect(directive).toBeTruthy();
  });
});
